@page
@model AspNetCoreWebApp.Pages.m_db1.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.m_t1[0].m_c1_text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.m_t1[0].m_c2_decimal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.m_t1[0].m_c3_date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.m_t1) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.m_c1_text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.m_c2_decimal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.m_c3_date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.m_id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.m_id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.m_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
